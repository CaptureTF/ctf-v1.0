<%= form_with(model: question, local: true) do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question.errors.count, "error") %> prohibited this question from being saved:</h2>

      <ul>
      <% question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Question_ID %>
    <%= form.text_field :Question_ID, id: :question_Question_ID %>
  </div>

  <div class="field">
    <%= form.label :Problem_Name %>
    <%= form.text_field :Problem_Name, id: :question_Problem_Name %>
  </div>

  <div class="field">
    <%= form.label :Max_Points %>
    <%= form.text_field :Max_Points, id: :question_Max_Points %>
  </div>

  <div class="field">
    <%= form.label :Submissions %>
    <%= form.text_field :Submissions, id: :question_Submissions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
